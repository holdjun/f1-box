<div class="ask" transition:persist="ask-panel">
  <button class="ask__trigger" type="button" aria-expanded="false" aria-controls="ask-panel" aria-label="打开 F1 问答">
    <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true">
      <path
        d="M12 3a8 8 0 0 1 8 8 8 8 0 0 1-8 8 8 8 0 0 1-3.4-.76L5 20l.9-3.2A8 8 0 0 1 12 3Z"
        fill="none"
        stroke="currentColor"
        stroke-width="1.6"
      />
      <path d="M9.6 9.4a2.5 2.5 0 1 1 3.6 2.24c-.72.38-1.2.9-1.2 1.86" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" />
      <circle cx="12" cy="16.4" r="1" fill="currentColor" />
    </svg>
  </button>
  <section class="ask__panel" id="ask-panel" role="dialog" aria-label="F1 问答" hidden>
    <header class="ask__head">
      <h2 class="ask__title">F1 问答</h2>
      <p class="ask__note">统计数据：f1db（CC BY 4.0，非实时）</p>
      <button class="ask__clear" type="button" hidden>清空</button>
      <button class="ask__close" type="button" aria-label="关闭问答面板">✕</button>
    </header>
    <div class="ask__messages" aria-live="polite"></div>
    <p class="ask__status" hidden>正在查询数据…</p>
    <p class="ask__error" role="alert" hidden></p>
    <form class="ask__form">
      <textarea
        class="ask__input"
        rows="1"
        placeholder="问点什么，如：汉密尔顿哪几年夺冠？"
        maxlength="2000"
      ></textarea>
      <button class="ask__send" type="submit">发送</button>
      <button class="ask__stop" type="button" hidden>停止</button>
    </form>
  </section>
</div>

<script>
  import "../scripts/ask-panel.ts";
</script>

<style>
  .ask {
    position: fixed;
    right: 20px;
    bottom: 20px;
    z-index: 60;
    font-size: 15px;
  }
  .ask__trigger {
    display: grid;
    place-items: center;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    border: 1px solid var(--line);
    background: var(--ink-soft);
    color: var(--paper);
    cursor: pointer;
    transition: border-color 160ms ease, color 160ms ease;
  }
  .ask__trigger:hover {
    border-color: var(--acid);
    color: var(--acid);
  }
  .ask__panel {
    position: fixed;
    right: 20px;
    bottom: 88px;
    display: flex;
    flex-direction: column;
    width: min(400px, calc(100vw - 40px));
    height: min(560px, calc(100dvh - 120px));
    border: 1px solid var(--line);
    border-radius: 12px;
    background: var(--ink-soft);
    overflow: hidden;
  }
  .ask__panel[hidden] {
    display: none;
  }
  .ask__head {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 14px;
    border-bottom: 1px solid var(--line);
  }
  .ask__title {
    margin: 0;
    font-family: var(--display);
    font-size: 18px;
    letter-spacing: 0.04em;
  }
  .ask__note {
    flex: 1;
    margin: 0;
    color: var(--muted);
    font-size: 11px;
  }
  .ask__clear,
  .ask__close {
    min-width: 44px;
    min-height: 44px;
    border: none;
    background: none;
    color: var(--muted-light);
    cursor: pointer;
  }
  .ask__messages {
    flex: 1;
    overflow-y: auto;
    padding: 14px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .ask__bubble {
    max-width: 86%;
    padding: 10px 12px;
    border-radius: 10px;
    border: 1px solid var(--line);
    white-space: pre-wrap;
    overflow-wrap: anywhere;
    line-height: 1.55;
  }
  .ask__bubble--user {
    align-self: flex-end;
    background: var(--coral);
    border-color: var(--coral);
    color: var(--ink);
  }
  .ask__bubble--assistant {
    align-self: flex-start;
    background: var(--ink);
    color: var(--paper);
  }
  .ask__status,
  .ask__error {
    margin: 0;
    padding: 6px 14px;
    color: var(--muted);
    font-size: 13px;
  }
  .ask__error {
    color: var(--coral);
  }
  .ask__form {
    display: flex;
    gap: 8px;
    padding: 12px;
    border-top: 1px solid var(--line);
  }
  .ask__input {
    flex: 1;
    min-height: 44px;
    resize: none;
    padding: 10px 12px;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--ink);
    color: var(--paper);
    font: inherit;
  }
  .ask__send,
  .ask__stop {
    min-width: 64px;
    min-height: 44px;
    border-radius: 8px;
    border: 1px solid var(--acid);
    background: var(--acid);
    color: var(--ink);
    font: inherit;
    cursor: pointer;
  }
  .ask__stop {
    border-color: var(--coral);
    background: var(--coral);
  }
  @media (max-width: 420px) {
    .ask {
      right: 12px;
      bottom: 12px;
    }
    .ask__panel {
      right: 0;
      bottom: 0;
      width: 100vw;
      height: 100dvh;
      border-radius: 0;
      border: none;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .ask__trigger,
    .ask__send,
    .ask__stop {
      transition: none;
    }
  }
</style>
