{
  "name": "@f1-box/weather-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir dist --containers-rollout none"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260714.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "4.112.0"
  }
}
