# 车队/车手目录与详情页年份筛选

日期：2026-08-13
状态：已实现

## 背景与目标

teams/drivers 目录页是全量名单，详情页的逐年成绩矩阵一次渲染全部赛季。用户需要按年份切片：目录页只看某一年参赛的车队/车手，详情页只显示选定一个或多个年份的成绩表。

## 用户可见行为

- /drivers、/teams 顶部新增年份选择栏（All 默认全量 + 1950–2026 降序），点击某年只显示该年参赛车手/车队，卡片显示该年车队；URL 为 ?year=YYYY，可分享可后退。
- 目录排序按积分：全量按生涯总积分降序，单年按该年积分降序；无积分榜的（如替补）垫底按名字排。
- 车手卡片号码：永久车号优先，无永久车号的车手回落最后参赛号码（1974 前号码按站分配，仍显示名字缩写）；年份视图显示该年当时的号码。
- /drivers/{id}、/teams/{slug} 的 Season results 上方新增筛选栏（All + 该实体参赛年份降序），可单选、多选、切回全部；选中后只显示对应年份的成绩表，断档提示、转队分隔线跟随所属赛季一起显隐。
- 详情页筛选状态写入 URL（?year=1997,2007），可分享、刷新保持、后退恢复。
- 同 PR 附带车手详情页矩阵重构：车手表改为车手+队友行（队友行灰色且可链接）、多年多队拆成连续多表并加转队连线、积分与夺冠标记只在首表显示避免拆表重复。

## 验收标准

- driver-repository / team-repository 单测：按年查询 SQL 含 test_driver=0 与年份过滤、积分排序、行映射、去重；年份列表降序；DEV fixture 按积分排序、号码回落逻辑。
- e2e：目录页 ?year=1997 名单被过滤且按该年积分排序、默认全量按总积分排序；无永久车号车手显示最后号码，1974 前退役车手回落 monogram；详情页点单年份只剩该 block、多选、All 恢复；转队分隔线随筛选显隐；375px 无横向溢出。
- DEV fixture 补 seasons 字段（积分与逐年号码，脚本从远程 D1 生成），DEV 下按年查询与排序可用。
- pnpm check / pnpm test / build / e2e 通过；preview 目检桌面与 375px。

## 范围外

- 卡片上的逐年积分/排名文案。
